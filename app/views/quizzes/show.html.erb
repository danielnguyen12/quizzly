<p style="color: green"><%= notice %></p>

<%= render @quiz %>

<div>
  <%= link_to "Back to quizzes", quizzes_path, class:'btn btn-primary' %>
  <%= link_to 'Download/Print PDF', quiz_path(format: :pdf), class:'btn btn-primary' %>

  <%= button_to "Delete this quiz", @quiz, method: :delete, class:'btn btn-danger' %>
</div>
